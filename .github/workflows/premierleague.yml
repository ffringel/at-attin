name: "Premier League"
on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:
jobs:
  build:
    uses: ./.github/workflows/post-bot.yml
    with:
      bskyHandle: "premierleaguebot.bsky.social"
      mastodonHandle: "@premierleague@sportsbots.xyz"
      mastodonAccountId: "110252020252170045"
    secrets:
      bskyPassword: ${{ secrets.PREMIER_LEAGUE_BSKY_PASSWORD }}