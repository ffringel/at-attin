/**
 * @at-attin/mastodon - Mastodon API client for fetching posts
 *
 * Only MastodonService is part of the public API. The config/ and utils/
 * modules are internal implementation details and are not re-exported here.
 */
export { default } from './services/mastodonService.js';